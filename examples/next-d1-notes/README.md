# Next D1 notes example

This example is the manual test bench for the package's generated Next routes,
IndexedDB row client, service worker, and Cloudflare D1 authority.

Run it from the repository root:

```bash
npm run dev
```

The root script builds the package CLI, regenerates this example from
`sync.next.config.json`, then starts Vinext with this directory as the app root.
